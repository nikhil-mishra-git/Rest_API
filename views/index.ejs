<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>API Documentation</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
</head>

<body>
    <div class="container">
        <header>
            <h1>Product Management API Documentation</h1>
            <p>Your guide to using the Product Management API for CRUD operations.</p>
        </header>

        <section id="base-url">
            <h2>Base URL</h2>
            <p><code>http://localhost:3000/api</code></p>
        </section>

        <section id="endpoints">
            <h2>Endpoints</h2>

            <div class="endpoint">
                <h3>Get All Products</h3>
                <p><strong>Endpoint:</strong> <code>/products</code></p>
                <p><strong>Method:</strong> GET</p>
                <p><strong>Description:</strong> Retrieves a list of all products.</p>

                <div class="example">
                    <h4>Example Request:</h4>
                    <pre><code>GET /api/products</code></pre>
                </div>

                <div class="example">
                    <h4>Response:</h4>
                    <pre><code>[
    {
        "productId": "101",
        "productName": "Wireless Mouse",
        "productDesc": "Ergonomic wireless mouse with Bluetooth connectivity.",
        "productPrice": "1200",
        "productQty": "50"
    },
    {
        "productId": "102",
        "productName": "Mechanical Keyboard",
        "productDesc": "RGB backlit mechanical keyboard with blue switches.",
        "productPrice": "2000",
        "productQty": "30"
    }
]</code></pre>
                </div>
            </div>

            <div class="endpoint">
                <h3>Get Product by ID</h3>
                <p><strong>Endpoint:</strong> <code>/products/:id</code></p>
                <p><strong>Method:</strong> GET</p>
                <p><strong>Description:</strong> Retrieves a single product by its ID.</p>

                <div class="example">
                    <h4>Example Request:</h4>
                    <pre><code>GET /api/products/101</code></pre>
                </div>

                <div class="example">
                    <h4>Response:</h4>
                    <pre><code>{
    "productId": "101",
    "productName": "Wireless Mouse",
    "productDesc": "Ergonomic wireless mouse with Bluetooth connectivity.",
    "productPrice": "1200",
    "productQty": "50"
}</code></pre>
                </div>
            </div>

            <div class="endpoint">
                <h3>Create a New Product</h3>
                <p><strong>Endpoint:</strong> <code>/products</code></p>
                <p><strong>Method:</strong> POST</p>
                <p><strong>Description:</strong> Creates a new product.</p>

                <div class="example">
                    <h4>Example Request:</h4>
                    <pre><code>POST /api/products
{
    "productId": "103",
    "productName": "Bluetooth Headphones",
    "productDesc": "Noise-cancelling Bluetooth headphones with long battery life.",
    "productPrice": "2500",
    "productQty": "25"
}</code></pre>
                </div>

                <div class="example">
                    <h4>Response:</h4>
                    <pre><code>{
    "message": "Product created successfully.",
    "productId": "103"
}</code></pre>
                </div>
            </div>

            <div class="endpoint">
                <h3>Update Product by ID</h3>
                <p><strong>Endpoint:</strong> <code>/products/:id</code></p>
                <p><strong>Method:</strong> PATCH</p>
                <p><strong>Description:</strong> Updates an existing product by its ID.</p>

                <div class="example">
                    <h4>Example Request:</h4>
                    <pre><code>PATCH /api/products/103
{
    "productPrice": "1600",
    "productQty": "20"
}</code></pre>
                </div>

                <div class="example">
                    <h4>Response:</h4>
                    <pre><code>{
    "message": "Product updated successfully."
}</code></pre>
                </div>
            </div>

            <div class="endpoint">
                <h3>Delete Product by ID</h3>
                <p><strong>Endpoint:</strong> <code>/products/:id</code></p>
                <p><strong>Method:</strong> DELETE</p>
                <p><strong>Description:</strong> Deletes a product by its ID.</p>

                <div class="example">
                    <h4>Example Request:</h4>
                    <pre><code>DELETE /api/products/103</code></pre>
                </div>

                <div class="example">
                    <h4>Response:</h4>
                    <pre><code>{
    "message": "Product deleted successfully."
}</code></pre>
                </div>
            </div>
        </section>

        <section id="error-handling">
            <h2>Error Handling</h2>
            <p>The API will return appropriate status codes and error messages in case of errors.</p>
            <pre><code>{
    "message": "Product not found."
}</code></pre>
        </section>

        <footer>
            <p>Product Management API</p>
        </footer>
    </div>
</body>

</html>